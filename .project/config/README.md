# Configuration

Technical configuration files for testing, builds, and optimization.

## Directory Structure

- `testing/` - Test configuration files
- `gains/` - PSO-optimized gain files
- `build/` - Build configuration (cliff.toml, linting configs)

## Purpose

Central repository for all technical configuration files.
